# Playlist manifests

These files preserve playlist identity, order, repeated positions, and progress. They are not summaries. The machine-readable JSON manifests are authoritative for validation.

Raw URL snapshots are retained under `raw/` when available.
