# Sources

- [`videos/`](videos/) contains the canonical, one-file-per-video records.
- [`playlists/`](playlists/) contains membership/order manifests and raw acquisition snapshots.

A playlist is an acquisition container, not the unit of evidence. See [`../SOURCE_ARCHITECTURE.md`](../SOURCE_ARCHITECTURE.md).
